const os = require("os");

// os module is used to ge information about system from user using various os functions using dot (.) operator. 


// console.log(os);
console.log(os.userInfo());
console.log(os.uptime());
console.log(os.freemem());